package org.stofkat.battleround.common.resources;

/**
 * Just a simple enum to put the resources in categories.  
 * @author Leejjon
 */
public enum ResourceCategory {
	Database, General, Level, Login, Logout, Register;
}
